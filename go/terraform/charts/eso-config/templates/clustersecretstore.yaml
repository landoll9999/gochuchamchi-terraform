apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: aws-secretsmanager
spec:
  provider:
    aws:
      service: SecretsManager
      region: "{{ .Values.region }}"
      # auth 블록 없음 = 컨트롤러 파드의 기본 자격증명 체인 사용
      # -> eks-pod-identity(eso.tf)가 붙여준 IAM 역할로 인증됨
