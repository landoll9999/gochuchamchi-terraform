================================================================================
ECR State 분리 매뉴얼
================================================================================

작성일 : 2026-07-31
대상   : gochuchamchi 인프라 (계정 307223751140, ap-northeast-2)

!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
주의 (2026-08-05)  →  해소됨 (2026-08-06, 아래 참조)
이 문서는 이미지 서명 도입 전의 MUTABLE/latest 구조를 기준으로 작성됐다.
현재 ECR에는 불변 태그, Cosign 참조 아티팩트 예외, KMS 키와 서명 IAM 역할이 있으므로
이 매뉴얼을 그대로 실행하면 공급망 보안 구성이 누락되거나 되돌아갈 수 있다.
state 분리는 팀 합의 후 현재 ecr.tf의 서명·Lifecycle 구성을 함께 옮기는 방식으로 재설계할 것.
!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!

★ 실행 완료 — 2026-08-06 ★

  위 주의사항이 요구한 대로 "서명 구성을 함께 옮기는 방식"으로 재설계해서
  실행했습니다. 계획서 원문과 달라진 점은 다음과 같습니다.

  1) 디렉토리 이름이 go/ecr 이 아니라 go/persistent 입니다.
     옮긴 대상이 ECR만이 아니라서 이름을 바꿨습니다.

  2) 옮긴 리소스가 5개가 아니라 10개입니다.
     2026-08-06 재구축에서 ECR 말고도 같은 성격의 사고가 둘 더 드러났습니다.
       - 서명 KMS 키가 재생성 -> GitHub 변수가 옛 키(PendingDeletion)를 가리킴
         -> CI 서명 잡 실패 -> ECR에 이미지가 안 올라감 -> 503 유지
       - ArgoCD PAT 시크릿이 삭제(recovery_window 0) -> 값 소실
         -> ArgoCD가 private GitOps 저장소 인증 실패 -> 앱 미배포 -> 503
     이미지만 살려도 CI를 못 돌리면 갱신할 수 없고, CI가 돌아도 PAT가 없으면
     배포가 안 됩니다. 그래서 넷을 한 묶음으로 옮겼습니다.

     추가된 5개:
       aws_kms_key.image_signing
       aws_kms_alias.image_signing
       aws_iam_role.github_actions_image_signer
       aws_iam_role_policy.github_actions_image_signer
       aws_secretsmanager_secret.argocd_git_pat

  3) 공급망 보안 구성(PR #5, feat/ecr-tag-immutability)을 그대로 이관했습니다.
     불변 태그(IMMUTABLE_WITH_EXCLUSION) + Cosign 아티팩트 3종 예외 필터와
     3단계 lifecycle 정책(signed 20개 보존 / candidate 7일 / untagged 7일)이
     go/persistent/ecr.tf 에 있습니다. 위 주의사항이 경고한 "누락되거나
     되돌아감"이 일어나지 않도록, 분리 직후 그 구성을 반영해 apply했습니다.

     ※ PR #5는 머지만 되고 apply되지 않은 상태였습니다(실물 ECR은 MUTABLE +
       lifecycle 1규칙). 그래서 분리 시점의 import는 실물 기준으로 잡혔고,
       불변 태그 전환은 별도 apply로 적용했습니다.

  실행 방식도 계획서의 state mv 대신 "persistent에서 import -> 메인에서
  state rm" 순서를 썼습니다. 두 state에 잠시 동시 존재할 뿐 apply를 하지
  않으므로, 순서를 잘못 밟아 ECR이 파괴될 위험이 없습니다.

  검증 결과 — 메인 plan에서 "0 to change, 실제 인프라 destroy 0건"
  (재실행 전용 null_resource 3개만 교체됨).

  현재 절차는 docs/runbook.md §4 를 보세요.
  아래 원문은 당시 계획 기록으로 남겨 둡니다.


--------------------------------------------------------------------------------
1. 왜 하는가
--------------------------------------------------------------------------------

terraform/ecr.tf 의 aws_ecr_repository 에는 force_delete = true 가 걸려 있다.
따라서 terraform destroy 를 하면 ECR 저장소가 푸시된 이미지까지 통째로 삭제된다.

재구축 후에는 ECR 이 빈 상태로 다시 생성되는데, GitOps 저장소의 Deployment 는
여전히 예전 이미지 태그를 참조하므로 Pod 가 ImagePullBackOff 에 빠지고
ALB 가 503 을 반환한다. (2026-07-31 실제 발생)

이때마다 gochuchamchi-spring 저장소의 CI 를 수동으로 다시 돌려야 한다.

ECR 을 별도 state 로 분리하면 메인 인프라를 destroy/apply 해도 이미지가 살아남아
재구축 직후 Pod 가 바로 뜬다.


--------------------------------------------------------------------------------
2. 최종 구조
--------------------------------------------------------------------------------

go/
├── terraform/                  메인 인프라 (VPC/EKS/RDS/ArgoCD ...)
│   └── ecr.tf                  → 삭제
│   └── ecr-data.tf             → 신규 (data source 로 ECR 참조)
├── ecr/                        신규 디렉토리 (state 분리)
│   ├── backend.tf
│   ├── providers.tf
│   ├── variables.tf
│   └── ecr.tf
├── discord-notifications/      (기존, 동일한 state 분리 패턴)
└── cloudwatch-notifications/   (기존)

옮기는 리소스 5개
  - aws_ecr_repository.gochuchamchi
  - aws_ecr_lifecycle_policy.gochuchamchi
  - aws_iam_openid_connect_provider.github_actions
  - aws_iam_role.github_actions_ecr_push
  - aws_iam_role_policy.github_actions_ecr_push

메인 state 에 그대로 두는 것
  - module.image_updater_ecr_pod_identity
    (AWS 관리형 정책 AmazonEC2ContainerRegistryReadOnly 만 참조하므로 무관)


--------------------------------------------------------------------------------
3. 사전 확인
--------------------------------------------------------------------------------

  cd C:\terraform\go\go\terraform
  $env:AWS_PROFILE = "admin"

  terraform state list | Select-String "ecr|github_actions"

다음 5개가 나와야 한다.

  aws_ecr_lifecycle_policy.gochuchamchi
  aws_ecr_repository.gochuchamchi
  aws_iam_openid_connect_provider.github_actions
  aws_iam_role.github_actions_ecr_push
  aws_iam_role_policy.github_actions_ecr_push

state 백업 (문제 생기면 되돌릴 근거):

  terraform state pull > ..\docs\backup-main-state-20260731.json

※ 이 백업 파일에는 RDS 비밀번호 등 민감값이 들어있다. git 에 커밋하지 말 것.
   작업이 끝나면 삭제한다.


--------------------------------------------------------------------------------
4. 생성할 파일
--------------------------------------------------------------------------------

================================ go/ecr/backend.tf
# Terraform 1.10+: DynamoDB 없이 S3 네이티브 잠금(use_lockfile) 사용
terraform {
  backend "s3" {
    bucket       = "gochuchamchi-tfstate-307223751140"
    key          = "ecr/terraform.tfstate"
    region       = "ap-northeast-2"
    profile      = "admin"
    encrypt      = true
    use_lockfile = true
  }
}

================================ go/ecr/providers.tf
terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
    tls = {
      source  = "hashicorp/tls"
      version = "~> 4.0"
    }
  }
}

provider "aws" {
  region  = var.region
  profile = var.aws_profile
}

================================ go/ecr/variables.tf
variable "aws_profile" {
  description = "AWS CLI profile (../terraform/variables.tf와 동일 값 사용)"
  type        = string
  default     = "admin"
}

variable "region" {
  type    = string
  default = "ap-northeast-2"
}

variable "github_owner" {
  description = "CI 워크플로가 위치한 GitHub 계정/조직 (../terraform의 argocd_github_owner와 동일 값)"
  type        = string
  default     = "landoll9999"
}

================================ go/ecr/ecr.tf
# =============================================================================
# ECR — 컨테이너 이미지 레지스트리 + GitHub Actions CI push 권한
#
# ../terraform 과 state 를 분리한 이유:
#   메인 인프라를 destroy 하면 force_delete 로 이미지가 전부 삭제되어, 재구축 후
#   GitOps 매니페스트가 가리키는 태그를 못 찾아 Pod 가 ImagePullBackOff 에 빠진다.
#   여기로 떼어내면 메인을 destroy/apply 해도 이미지가 살아남는다.
#
# ※ 메인(../terraform)보다 먼저 apply 해야 한다.
#    메인의 ecr-data.tf 가 data source 로 이 저장소를 조회하기 때문.
# =============================================================================

resource "aws_ecr_repository" "gochuchamchi" {
  name                 = "gochuchamchi"
  image_tag_mutability = "MUTABLE" # CI/Image Updater가 latest 태그를 계속 덮어씀

  # 이 디렉토리는 destroy 대상이 아니지만, 만약 destroy 하게 되면 이미지가 남아
  # 있어도 지워지도록 둔다. 실수 방지는 아래 prevent_destroy 가 담당.
  force_delete = true

  # 이 state 를 분리한 목적 자체가 이미지 보존이므로 실수로 지우는 것을 막는다.
  # 정말 삭제해야 한다면 이 블록을 먼저 지우고 destroy 할 것.
  lifecycle {
    prevent_destroy = true
  }

  image_scanning_configuration {
    scan_on_push = true
  }
}

resource "aws_ecr_lifecycle_policy" "gochuchamchi" {
  repository = aws_ecr_repository.gochuchamchi.name

  policy = jsonencode({
    rules = [{
      rulePriority = 1
      description  = "최근 10개 이미지만 유지, 나머지는 자동 만료"
      selection = {
        tagStatus   = "any"
        countType   = "imageCountMoreThan"
        countNumber = 10
      }
      action = { type = "expire" }
    }]
  })
}

# ---------------------------------------------------------------------------
# GitHub Actions(gochuchamchi-spring 저장소)가 OIDC로 AWS를 assume해서 ECR에
# push. 액세스키를 GitHub Secret에 저장할 필요가 없어 Docker Hub 방식(계정+토큰)
# 보다 안전함.
# ---------------------------------------------------------------------------
data "tls_certificate" "github_actions" {
  url = "https://token.actions.githubusercontent.com/.well-known/openid-configuration"
}

resource "aws_iam_openid_connect_provider" "github_actions" {
  url             = "https://token.actions.githubusercontent.com"
  client_id_list  = ["sts.amazonaws.com"]
  thumbprint_list = [data.tls_certificate.github_actions.certificates[0].sha1_fingerprint]
}

resource "aws_iam_role" "github_actions_ecr_push" {
  name = "gochuchamchi-github-actions-ecr-push"

  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = { Federated = aws_iam_openid_connect_provider.github_actions.arn }
      Action    = "sts:AssumeRoleWithWebIdentity"
      Condition = {
        StringEquals = {
          "token.actions.githubusercontent.com:aud" = "sts.amazonaws.com"
        }
        # main 브랜치 push(=CI 워크플로 트리거 조건)에서만 assume 가능하도록 제한
        StringLike = {
          "token.actions.githubusercontent.com:sub" = "repo:${var.github_owner}/gochuchamchi-spring:ref:refs/heads/main"
        }
      }
    }]
  })
}

resource "aws_iam_role_policy" "github_actions_ecr_push" {
  name = "ecr-push"
  role = aws_iam_role.github_actions_ecr_push.id

  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid      = "EcrAuth"
        Effect   = "Allow"
        Action   = "ecr:GetAuthorizationToken"
        Resource = "*"
      },
      {
        Sid    = "EcrPush"
        Effect = "Allow"
        Action = [
          "ecr:BatchCheckLayerAvailability",
          "ecr:GetDownloadUrlForLayer",
          "ecr:BatchGetImage",
          "ecr:PutImage",
          "ecr:InitiateLayerUpload",
          "ecr:UploadLayerPart",
          "ecr:CompleteLayerUpload",
        ]
        Resource = aws_ecr_repository.gochuchamchi.arn
      }
    ]
  })
}

output "ecr_repository_url" {
  value       = aws_ecr_repository.gochuchamchi.repository_url
  description = "gochuchamchi-gitops의 이미지 참조와 gochuchamchi-spring CI 워크플로의 push 대상으로 사용"
}

output "github_actions_ecr_role_arn" {
  value       = aws_iam_role.github_actions_ecr_push.arn
  description = "gochuchamchi-spring CI 워크플로의 aws-actions/configure-aws-credentials role-to-assume 값으로 사용"
}

================================ go/ecr/imports.tf  (임시 — 6단계에서 삭제)
# 기존 AWS 리소스를 이 state 로 가져오기 위한 임시 파일.
# import 완료 후 삭제한다. (남겨둬도 동작은 하지만 불필요한 노이즈)

import {
  to = aws_ecr_repository.gochuchamchi
  id = "gochuchamchi"
}

import {
  to = aws_ecr_lifecycle_policy.gochuchamchi
  id = "gochuchamchi"
}

import {
  to = aws_iam_openid_connect_provider.github_actions
  id = "arn:aws:iam::307223751140:oidc-provider/token.actions.githubusercontent.com"
}

import {
  to = aws_iam_role.github_actions_ecr_push
  id = "gochuchamchi-github-actions-ecr-push"
}

import {
  to = aws_iam_role_policy.github_actions_ecr_push
  id = "gochuchamchi-github-actions-ecr-push:ecr-push"
}

================================ go/terraform/ecr-data.tf  (신규)
# =============================================================================
# ECR 은 ../ecr 디렉토리가 별도 state 로 관리한다 (destroy 시 이미지 보존 목적).
# 여기서는 조회만 한다 -> ../ecr 를 먼저 apply 해야 이 data source 가 성공한다.
# =============================================================================

data "aws_ecr_repository" "gochuchamchi" {
  name = "gochuchamchi"
}


--------------------------------------------------------------------------------
5. 수정할 파일 (go/terraform/argocd.tf)
--------------------------------------------------------------------------------

13행
  이전 : ecr_registry_host = split("/", aws_ecr_repository.gochuchamchi.repository_url)[0]
  이후 : ecr_registry_host = split("/", data.aws_ecr_repository.gochuchamchi.repository_url)[0]

108행
  이전 : appImage = aws_ecr_repository.gochuchamchi.repository_url
  이후 : appImage = data.aws_ecr_repository.gochuchamchi.repository_url

4행 주석의 "ecr.tf의" 는 "../ecr/ecr.tf의" 로 바꿔두면 좋다.


--------------------------------------------------------------------------------
6. 실행 순서
--------------------------------------------------------------------------------

>>> 순서를 반드시 지킬 것. 특히 [4단계] 보다 [5단계] 를 먼저 하면 안 된다. <<<
>>> ecr.tf 를 지운 상태에서 메인 apply 를 돌리면 Terraform 이 ECR 저장소를    <<<
>>> 파괴하고, force_delete = true 라 이미지까지 전부 삭제된다.                <<<


[1단계] 새 디렉토리와 파일 생성
  4장의 파일 5개를 만든다. (backend / providers / variables / ecr / imports)

[2단계] 새 state 초기화
  cd C:\terraform\go\go\ecr
  $env:AWS_PROFILE = "admin"
  terraform init

[3단계] import 실행
  terraform plan

  → "5 to import, 0 to add, 0 to change, 0 to destroy" 여야 한다.
    to add / to destroy 가 0 이 아니면 여기서 멈추고 원인을 확인할 것.
    (OIDC thumbprint 는 값이 갱신되어 to change 1 이 나올 수 있으며 무해하다)

  terraform apply

[4단계] 메인 state 에서 추적 해제
  ※ state rm 은 AWS 리소스를 건드리지 않는다. Terraform 이 잊을 뿐이다.

  cd C:\terraform\go\go\terraform
  terraform state rm aws_ecr_repository.gochuchamchi
  terraform state rm aws_ecr_lifecycle_policy.gochuchamchi
  terraform state rm aws_iam_openid_connect_provider.github_actions
  terraform state rm aws_iam_role.github_actions_ecr_push
  terraform state rm aws_iam_role_policy.github_actions_ecr_push

[5단계] 메인 파일 정리
  - go/terraform/ecr.tf 삭제
  - go/terraform/ecr-data.tf 생성 (4장 참고)
  - go/terraform/argocd.tf 수정 (5장 참고)

[6단계] 임시 파일 삭제
  - go/ecr/imports.tf 삭제

[7단계] 검증
  cd C:\terraform\go\go\terraform
  terraform plan

  → "No changes" 여야 한다.
    ECR 관련 create/destroy 가 하나라도 보이면 8장(문제 대응)으로.


--------------------------------------------------------------------------------
7. 검증
--------------------------------------------------------------------------------

이미지가 그대로인지:
  aws ecr list-images --repository-name gochuchamchi `
    --profile admin --region ap-northeast-2 --query "imageIds[].imageTag" --output text

새 state 에 5개가 들어갔는지:
  cd C:\terraform\go\go\ecr
  terraform state list

메인 state 에서 빠졌는지 (아무것도 안 나와야 함):
  cd C:\terraform\go\go\terraform
  terraform state list | Select-String "ecr_repository|ecr_lifecycle|github_actions"

CI 역할 ARN 이 그대로인지 (GitHub Actions 워크플로 설정과 일치해야 함):
  cd C:\terraform\go\go\ecr
  terraform output github_actions_ecr_role_arn
  → arn:aws:iam::307223751140:role/gochuchamchi-github-actions-ecr-push
    (역할 이름이 고정이라 ARN 은 바뀌지 않는다. GitHub 쪽 수정 불필요)


--------------------------------------------------------------------------------
8. 문제 대응 / 되돌리기
--------------------------------------------------------------------------------

[3단계에서 to add 나 to destroy 가 보인다]
  import id 가 틀렸을 가능성이 높다. apply 하지 말고 확인할 것.
  OIDC provider ARN 은 계정 ID 가 맞는지 확인:
    aws iam list-open-id-connect-providers --profile admin

[4단계 이후 되돌리고 싶다]
  메인 state 로 다시 import 하면 된다. ecr.tf 를 복원한 뒤:
    cd C:\terraform\go\go\terraform
    terraform import aws_ecr_repository.gochuchamchi gochuchamchi
    terraform import aws_ecr_lifecycle_policy.gochuchamchi gochuchamchi
    terraform import aws_iam_openid_connect_provider.github_actions `
      arn:aws:iam::307223751140:oidc-provider/token.actions.githubusercontent.com
    terraform import aws_iam_role.github_actions_ecr_push gochuchamchi-github-actions-ecr-push
    terraform import aws_iam_role_policy.github_actions_ecr_push `
      gochuchamchi-github-actions-ecr-push:ecr-push
  그리고 go/ecr 의 state 를 비운다 (또는 S3 에서 ecr/terraform.tfstate 삭제).

[7단계에서 ECR 을 destroy 하려 한다]
  ecr-data.tf 가 없거나 argocd.tf 가 아직 resource 를 참조하고 있다.
  절대 apply 하지 말고 5장을 다시 확인할 것.

[state lock 이 걸렸다]
  terraform force-unlock <LOCK_ID>


--------------------------------------------------------------------------------
9. 이후 운영 방식 변경점
--------------------------------------------------------------------------------

apply 순서 (신규 구축 시)
  1) cd go/ecr        && terraform apply
  2) cd go/terraform  && terraform apply
  3) cd go/discord-notifications && terraform apply   (기존과 동일)

destroy 순서
  1) cd go/terraform && terraform destroy
  2) go/ecr 는 destroy 하지 않는다 (이미지 보존이 목적).
     prevent_destroy 때문에 시도해도 막힌다.

재구축 후
  ECR 이미지가 살아있으므로 CI 재실행 없이 Pod 가 바로 뜬다.
  단, GitOps 매니페스트의 태그가 ECR 라이프사이클 정책(최근 10개 유지)으로
  만료된 경우에는 여전히 CI 재실행이 필요하다.

메인 apply 시 주의
  go/ecr 를 apply 하지 않은 상태에서 메인을 apply 하면
  data.aws_ecr_repository 조회 실패로 plan 단계에서 멈춘다. 정상 동작이다.


--------------------------------------------------------------------------------
10. 참고
--------------------------------------------------------------------------------

- 동일한 state 분리 패턴 : go/discord-notifications/ , go/cloudwatch-notifications/
- state 버킷            : s3://gochuchamchi-tfstate-307223751140
    eks/terraform.tfstate                   메인
    ecr/terraform.tfstate                   신규
    discord-notifications/terraform.tfstate
- AWS profile           : admin (backend.tf 에 명시됨. default 프로파일 아님)
- 3장에서 만든 state 백업 json 은 작업 완료 후 삭제할 것 (민감값 포함)

================================================================================
